<%= form_for(@input_product) do |f| %>
  <% if @input_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@input_product.errors.count, "error") %> prohibited this input_product from being saved:</h2>

      <ul>
      <% @input_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stock, "Cantidad" %><br>
    <%= f.number_field :stock %>
  </div>
  <div class="field">
    <%= label_tag "Producto" %><br>
    <!--Treamos la informacion del producto-->
    <%= text_field_tag '',@product.name, disabled: false %>
  </div>
  <div>
    <!--Se trae la informacion del precio del produto-->
    <%= label_tag "Precio" %><br> 
    <%= text_field_tag @product.price,@product.price, disabled: false %><br> 
  </div>
  <div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
